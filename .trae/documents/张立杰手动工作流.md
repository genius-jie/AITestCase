## 1. 用户备注
工作流：
   提示词有架构师（DDT测试数据管理智能体提示词）-先保存原始接口文档（+提示词）=原始契约文档（意图识别数据契约文档）-执行测试-（接口文档，jtl测试结果，测试数据csv）=更新接口契约文档（意图识别数据契约文档）+业务数据设计师（DDT流程分析与提示词优化建议）-再根据提示词+契约优化+追加数据（覆盖多场景）
交付文件：
   ### 首次交付 
   1. 通用提示词（DDT测试数据管理智能体提示词.md），用于测试数据设计
   3. 原始接口文档，包含提示词（意图识别_api_analysis.md）
   4. 原始契约文档（意图识别数据契约文档.md），用于规定接口输入输出数据格式和业务规则
   3. 原始测试数据CSV文件（intent_recognition_test_data_v2.csv）
   5. 原始接口脚本（intent_recognition_jmeter.jmx）
   6. 分析执行报告（intent_recognition_results.jtl）
   ### 人工确认
   1. 检查执行报告是否符合预期，是否有异常情况。
   2. 确认测试数据是否覆盖了所有场景，是否有缺失或错误的数据。
   3. 哪些断言失败，是由于业务逻辑错误导致的？哪些是由于接口问题导致的？，确定脚本问题，还是断言有误，还是系统bug。
   ### 优化交付
   6. 意图识别优化提示词（意图识别DDT优化提示词.md），用于根据执行结果和业务要求，断言逻辑规则，具体输入输出规则。
   2. 更新后的契约文档（意图识别数据契约文档.md）
   3. 更新后的测试数据文件（intent_recognition_test_data_v2.csv）
   4. 更新后的JMeter测试脚本（intent_recognition_jmeter.jmx）
   5. 测试结果JTL文件（intent_recognition_results.jtl）
   6. 测试分析报告（test_analysis_report.md）
   ### 最终交付
   1、补充扩展测试数据100条（intent_recognition_test_data_v3.csv）
   2、更新脚本支持不同环境（如开发、测试、生产）的配置切换
   3、增加整体项目介绍和用户视角的工作流文档。
   4、最终版意图识别优化提示词（包含了工作流，面相agent的文档）


   通用错题本保存在个人规则中了
   创建新项目先执行下脚本创建模版化结构，generate_agent_three_pieces，bootstrap_phase_three_pieces
   请你参考 solo_execute.yaml 的规则和流程，创建语音生成自动化测试项目框架和文档体系
   这些生成的文件构成了一个 可直接使用的 TTS 自动化测试项目模板 ，您可以基于此框架进行实际的测试执行和项目完善。

你可以让 Agent“以 6A 的阶段结构来执行本次任务”，
但必须同时声明：
👉 这是「Scenario-Level Design」，不是「Agent Design」。
也就是说：
✅ 允许：需求拆解、数据设计、多阶段分析
❌ 禁止：更新 role.prompt.md / workflow.md / mistakes.md

以 6A 结构完成本次任务分析与设计，
本次 6A 仅用于「Scenario-Level Design」，
不得用于 Agent 级设计，
不得更新任何 Agent 三件套文件。




1、agent根目录三件套Agent Design执行命令：提示词agent+project_design+6a工作流，完成agent根目录三件套的设计
2、参考别的solo_execute.yaml 生成新的项目的solo_execute.yaml 文件。
3、执行脚本，新项目先执行下脚本创建模版化结构，generate_agent_three_pieces，bootstrap_phase_three_pieces
4、请你参考 solo_execute.yaml 的规则和流程，创建xxx项目的提示词工程

==========以上都是特定领域的抽象提示词工程，比如接口测试、效果评测、测试用例编写等，他们工作流都不同=====

=======以下是具体迭代的需求文档，
5、agent Scenario-Level Design执行命令：开始"Scenario-Level Design"，不是"Agent Design"，不允许更新role.prompt.md/workflow.md/mistakes.md，传入需求文档（保存在项目中）。此时A4就应该有数据契约文档了，用于规定接口输入输出数据格式和业务规则。以及各阶段的工作流。存储项目级规则

6、agent Execution 命令：进入 Execution 阶段，执行 Scenario-Level Design 的产出链路，继续推进项目后续阶段的执行工作。
7、执行过程中的总结，
   具体的迭代规则，分阶段（工作流，错题本，数据契约），计入**项目文件夹**下的Scenario-Level Design目录，每个阶段一个文档，比如E:\AI测试用例\接口测试\性能测试-OTA\docs\A5_测试执行.md。
   通用规则（工作流，错题本），集中一个文档管理，比如JMeter脚本设计规范，后来放入**agent**目录下，要求整合到agent各阶段的三件套规则中。